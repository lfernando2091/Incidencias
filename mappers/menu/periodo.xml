<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="periodo">

  <select id="catalogo">
    SELECT
      pkey Id,
      nombre Nombre,
      clave Clave,
      estatus Estatus
    FROM
      periodo
    WHERE
      estatus = 'activo'
  </select>

  <select id="findById">
    SELECT
      pkey Id,
      nombre Nombre,
      clave Clave,
      estatus Estatus
    FROM
      periodo
    WHERE
      pkey = ${id} AND estatus = 'activo'
  </select>

  <select id="findAllById">
    SELECT
      pkey Id,
      nombre Nombre,
      clave Clave,
      estatus Estatus
    FROM
      periodo
    WHERE
      pkey = ${id}
  </select>

  <insert id="agregarPeriodo">
        INSERT INTO periodo
        (
        nombre,
        clave
        )
        VALUES
        ('${nombre}', '${clave}')
    </insert>

</mapper>